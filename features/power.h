#ifndef LAGER_FEATURES_POWER_H
#define LAGER_FEATURES_POWER_H

#include "../features.h"

void feature_power_guest_setup(struct guest_ctx *ctx);
void feature_power_guest_stop(struct guest_ctx *ctx);

#endif
