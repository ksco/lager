#ifndef LAGER_FEATURES_BOX64_H
#define LAGER_FEATURES_BOX64_H

#include "../features.h"

void feature_box64_host_resolve(struct host_ctx *ctx);
void feature_box64_guest_setup(struct guest_ctx *ctx);

#endif
